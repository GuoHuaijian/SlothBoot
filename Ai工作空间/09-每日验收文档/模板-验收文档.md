# 验收文档模板

---

## 文档信息

| 项目 | 内容 |
|------|------|
| Day | [编号] |
| 日期 | [YYYY-MM-DD] |
| 主题 | [验收主题] |
| 关联计划 | [对应改造计划文档路径] |

---

## 一、验收标准总览

| 编号 | 验收项 | 类型 | 通过条件 | 结果 |
|------|--------|------|----------|------|
| V-01 | [验收项名称] | 自动/手动 | [明确的通过条件] | 通过/不通过 |
| V-02 | [验收项名称] | 自动/手动 | [明确的通过条件] | 通过/不通过 |

---

## 二、自动化检查项

### 2.1 编译检查

```bash
# 在项目根目录执行
mvn clean verify -DskipTests=false
```

**通过条件**：退出码为 0，无 ERROR 输出

**结果**： [ ] 通过 [ ] 不通过

---

### 2.2 静态分析检查

```bash
# Checkstyle 检查
mvn checkstyle:check -pl [受影响模块]

# SpotBugs 检查
mvn spotbugs:check -pl [受影响模块]
```

**通过条件**：零违规

**结果**： [ ] 通过 [ ] 不通过

---

### 2.3 测试检查

```bash
# 运行受影响模块的测试
mvn test -pl [受影响模块]
```

**通过条件**：所有测试通过，覆盖率不低于 [X]%

**结果**： [ ] 通过 [ ] 不通过

---

## 三、架构规范检查

### 3.1 包结构检查

**检查内容**：[具体检查什么]

**检查方法**：
```bash
# 示例：检查是否有跨模块的非法 import
grep -r "import com.sloth.boot.starter" sloth-boot-common/ | grep -v "test"
```

**通过条件**：[明确条件]

**结果**： [ ] 通过 [ ] 不通过

---

### 3.2 依赖方向检查

**检查内容**：[具体检查什么]

**检查方法**：
```bash
# 示例：检查 common 模块是否依赖 starter
grep -r "sloth-boot-starter" sloth-boot-common/*/pom.xml
```

**通过条件**：无反向依赖

**结果**： [ ] 通过 [ ] 不通过

---

## 四、功能回归检查

### 4.1 [功能点 1]

**操作步骤**：
1. [具体步骤]
2. [具体步骤]

**预期结果**：[明确的预期结果]

**实际结果**：[填写]

**结果**： [ ] 通过 [ ] 不通过

---

## 五、文档检查

- [ ] 改造计划文档已更新"实际完成情况"
- [ ] 相关规范文档已同步更新
- [ ] CHANGELOG 已更新
- [ ] 受影响模块的 README 已更新

---

## 六、验收结论

| 项目 | 结果 |
|------|------|
| 自动化检查 | [X/N 通过] |
| 架构规范检查 | [X/N 通过] |
| 功能回归检查 | [X/N 通过] |
| 文档检查 | [X/N 通过] |
| **总体结论** | **通过 / 不通过** |

**不通过项及整改计划**：
- [如果不通过，列出需要整改的内容和时间]

**验收人**：guohuaijian / AI

**验收日期**：[YYYY-MM-DD]
